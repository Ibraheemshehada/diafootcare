import 'package:flutter/material.dart';
class WoundHistoryScreen extends StatelessWidget {
  const WoundHistoryScreen({super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Wound History (design)'));
}
